import React from 'react'
import styled from 'styled-components'
import { title } from '../../../ultis/path'

function CreateService() {
  document.title = title.CREATE_CATEGORY
  return (
    <Styled></Styled>
  )
}

const Styled = styled.div`
    
`

export default CreateService