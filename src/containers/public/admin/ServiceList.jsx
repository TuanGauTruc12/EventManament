import React from 'react'
import styled from 'styled-components'
import { title } from '../../../ultis/path'

const ServiceList = () => {
  document.title = title.LIST_SERVICE
  return (
    <Styled></Styled>
  )
}

const Styled = styled.div`
    
`

export default ServiceList